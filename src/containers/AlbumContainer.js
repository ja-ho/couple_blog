import React, { PureComponent } from 'react';
import {Album} from '../components';

export default class AlbumContainer extends PureComponent {
    render() {
        return (
            <Album />
        );
    }

}
